# Leave unset for local development — Vite proxies /api to http://localhost:4000.
# Set this on a static host to the deployed API's origin, e.g.
# VITE_API_BASE=https://report-dispatch-api.onrender.com
VITE_API_BASE=
